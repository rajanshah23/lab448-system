# Copy this file to .env in the PROJECT ROOT (same folder as docker-compose.yml).
# This is NOT backend/.env — it is used only by Docker Compose.

# PostgreSQL — runs in a Docker container; you do NOT install PostgreSQL on your PC.
# These set the DB user, password, and database name inside the container.
POSTGRES_USER=lab448
POSTGRES_PASSWORD=lab448_secret
POSTGRES_DB=lab448_repair

# Backend (JWT signing)
JWT_SECRET=change-this-to-a-strong-secret-in-production

# Port on your machine where the app is reachable (Cloudflare Tunnel points here).
# Default 8080 works without admin; use 80 if you prefer and have permission.
HTTP_PORT=8080
