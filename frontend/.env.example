# Example frontend environment file for development
# Copy this to frontend/.env or frontend/.env.local

# Base API path (Vite will proxy /api to backend in dev)
VITE_API_BASE_URL="/api"

# NOTE: Do NOT put secrets (API keys, private tokens) in frontend env files.
# Frontend env files are bundled into the client and are public.
